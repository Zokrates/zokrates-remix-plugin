export const Example = {
    name: 'browser/example.code',
    content: 'def main(private field a, field b) -> (field):field\n\tresult = if a * a == b then 1 else 0 fi\n\treturn result'
};

export default Example;